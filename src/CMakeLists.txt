
IF(USE_OPENGL_GLUT)
    ADD_EXECUTABLE(hello_glut hello_glut.cc)

    TARGET_LINK_LIBRARIES(hello_glut ${OPENGL_LIBRARIES} ${GLUT_LIBRARIES})

    SET_TARGET_PROPERTIES(hello_glut PROPERTIES OUTPUT_NAME "hello_glut")
ENDIF()

IF(USE_OPENGL_GLFW)
    ADD_EXECUTABLE(hello_glfw hello_glfw.cc)

    TARGET_LINK_LIBRARIES(hello_glfw ${OPENGL_LIBRARIES} ${GLUT_LIBRARIES})

    SET_TARGET_PROPERTIES(hello_glfw PROPERTIES OUTPUT_NAME "hello_glfw")
ENDIF()

